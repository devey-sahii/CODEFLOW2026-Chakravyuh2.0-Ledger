from app.main import app

# This file serves as the entry point for Vercel's Serverless Functions.
# It imports the FastAPI `app` instance from `app.main` and exposes it
# to the Vercel Python runtime, ensuring `backend/requirements.txt` is detected.
